import 'package:flutter/material.dart';

class ScanSolvePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Scan & Solve'),
      ),
      body: Center(
        child: Text('This is the Scan & Solve Page'),
      ),
    );
  }
}
